// array: map,filter,reduce,sort, ...spread,...rest;

// map:  returns an new array 

let arr = [1,2,3,4,5];
let newCubeVal = function (num) {
    return num ** 3;
};

console.log(arr);
console.log(arr[0]);

let myEvenNo = function isEven(num) {
    if (num % 2 == 0) {
        return true;
    }
    return false;
};
let num = [1,2,3,4];
function myfunc(sum,nextvalue){
    return sum + nextValue;
}
console.log(num.reduce());
